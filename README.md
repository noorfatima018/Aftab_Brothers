# Aftab Brothers

Next.js App Router version of the luxury curtains landing page.

## Run

```bash
npm install
npm run dev
```

## Structure

- `app/` for the route entry points and global styles.
- `components/` for each page section.
- `lib/site-content.ts` for shared section data.
